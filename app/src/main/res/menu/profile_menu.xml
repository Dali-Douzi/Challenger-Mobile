<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- User's Teams Section -->
    <group android:id="@+id/group_teams">
        <item
            android:id="@+id/menu_team1"
            android:title="Team 1"
            tools:ignore="HardcodedText" />  <!-- Example team -->
        <item
            android:id="@+id/menu_team2"
            android:title="Team 2"
            tools:ignore="HardcodedText" />  <!-- Example team -->
        <!-- Add more teams dynamically from Firebase -->
    </group>

    <!-- Create Team Button -->
    <item
        android:id="@+id/menu_create_team"
        android:title="Create Team"
        tools:ignore="HardcodedText" />

    <!-- Logout Button -->
    <item
        android:id="@+id/menu_logout"
        android:title="Logout"
        tools:ignore="HardcodedText" />
</menu>
