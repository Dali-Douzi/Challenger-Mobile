<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Posting team name -->
    <TextView
        android:id="@+id/scrimTeamName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Team Name"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Posting team rank -->
    <TextView
        android:id="@+id/scrimTeamRank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rank: 1"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/scrimTeamName"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Date/time and format -->
    <TextView
        android:id="@+id/scrimTimeFormat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="May 14, 20:00 • Best of 3"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/scrimTeamRank"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Send/Sent/Edit/BOOKED button -->
    <Button
        android:id="@+id/scrimRequestButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send Scrim Request"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/scrimTimeFormat"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
